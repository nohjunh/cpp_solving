/*

// 헤더파일 중복 include를 피하기 위한
// 조건 컴파일 문 선언
#ifndef CALCULATOR_H
#define CALCULATOR_H

// Add 클래스
class Add {
	//멤버변수
	int a, b;
public:
	//멤버함수
	void setValue(int x, int y); // value값 셋팅
	int calculate();
};

// Sub 클래스
class Sub {
	//멤버변수
	int a, b;
public:
	//멤버함수
	void setValue(int x, int y);
	int calculate();
};

// Mul 클래스
class Mul {
	//멤버변수
	int a, b;
public:
	//멤버함수
	void setValue(int x, int y);
	int calculate();
};

// Div 클래스
class Div {
	//멤버변수
	int a, b;
public:
	//멤버함수
	void setValue(int x, int y);
	int calculate();
};

#endif // ! 조건 컴파일문 end

*/